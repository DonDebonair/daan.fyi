.astro/
dist/
node_modules/

# Content stays unformatted — decided, not deferred.
#
# 30 of the 33 MDX files already failed `prettier --check` before the migration, so
# formatting was never actually applied here. Running it now would rewrite prose in ways
# that can change rendered output (list indentation, hard breaks, table padding), against
# no benefit. The Phase 8 comparison is the reason to leave it alone, not a reason to
# revisit.
src/content/
